name: Build & Test

on:
  push:
    branches:
      - master

  pull_request:
    branches:
      - master

  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build and test'
        required: true
        default: 'master'

permissions:
  contents: read

env:
  # Enable colored output
  CTEST_OUTPUT_ON_FAILURE: 1

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        compiler:
          - g++
          - clang++
        preset:
          - debug-strict
          - release-strict

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || '' }}

      - name: Install dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
          sudo apt-get update

          wget https://apt.llvm.org/llvm.sh
          chmod +x ./llvm.sh
          sudo ./llvm.sh 20

          sudo apt-get install -y \
            build-essential \
            catch2 \
            clang-tidy-20 \
            cmake \
            cppcheck \
            gcc-14 \
            ninja-build

          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 1400
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 1400
          sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-20 2000
          sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-20 2000
          sudo update-alternatives --install /usr/bin/clang-tidy clang-tidy /usr/bin/clang-tidy-20 2000

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install \
            catch2 \
            cmake \
            cppcheck \
            gcc \
            ninja \
            llvm

          echo "$(brew --prefix llvm)/bin:/opt/homebrew/bin:$GITHUB_PATH" >>$GITHUB_PATH
          echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >>$GITHUB_ENV
          echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib/unwind -lunwind" >>$GITHUB_ENV

      - name: Configure with preset
        env:
          CXX: ${{ matrix.compiler }}
        run: |
          cmake --preset ${{ matrix.preset }}

      - name: Build with preset
        env:
          CXX: ${{ matrix.compiler }}
        run: |
          cmake --build --preset ${{ matrix.preset }} --parallel

      - name: Test with preset
        env:
          CXX: ${{ matrix.compiler }}
        run: |
          ctest --preset ${{ matrix.preset }} --parallel

      - name: Upload build logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ matrix.os }}-${{ matrix.compiler }}-${{ matrix.preset }}
          path: |
            build/${{ matrix.preset }}/
          retention-days: 3
